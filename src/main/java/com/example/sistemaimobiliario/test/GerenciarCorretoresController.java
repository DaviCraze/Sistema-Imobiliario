/*package com.example.sistemaimobiliario.test;

import com.example.sistemaimobiliario.usuario.Corretor;
import javafx.fxml.FXML;
import javafx.scene.control.TextField;

public class GerenciarCorretoresController {

    @FXML
    private TextField nomeField;
    @FXML
    private TextField cpfField;
    @FXML
    private TextField telefoneField;
    @FXML
    private TextField emailField;
    @FXML
    private TextField creciField;

    @FXML
    private void onAdicionarCorretorClick() {
        String nome = nomeField.getText();
        String cpf = cpfField.getText();
        String telefone = telefoneField.getText();
        String email = emailField.getText();
        String creci = creciField.getText();

        Corretor corretor = new Corretor(nome, cpf, telefone, email, creci);
        // Adicionar lógica para salvar o corretor
        System.out.println("Corretor adicionado: " + corretor.getNome());
    }
}*/